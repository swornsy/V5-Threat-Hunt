[defaults]
# Automatically defaults all target playbook maps to your subfolder inventory layout
inventory = inventory/hosts.ini

# Suppresses SSH/WinRM security alert banners during continuous training cycles
host_key_checking = False
